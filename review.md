> Обновлен: 11.08.2026

# Review

## Что исправил

Вынес спиннер загрузки в отдельный компонент `Loader` и убрал `setInterval`/`useEffect` — анимация символа `/` теперь крутится чисто на CSS . Стало меньше дублирования кода (компонент переиспользуется в `CardsList` и `CharacterDetail`) и меньше лишних ре-рендеров, при этом выглядит так же, как и раньше.

Разложил каждый компонент по своей папке (Card, CardsList, CharacterDetail, Loader). Думаю так удобнее искать компонент и связанные с ним файлы

Удалил src/data/cards.js — нигде не использовался, судя по всему моковые данные, которые забыли удалить

Переделал debounce поиска с ручного setTimeout/clearTimeout в отдельном стейте на useEffect с cleanup. Так React сам отменяет таймер при следующем вводе или размонтировании компонента, а не только на следующее нажатие клавиши, плюс ушёл лишний ре-рендер на каждую букву.

Удалил src/assets/rick_sanchez.jpg, morty_smith.jpg, evil_summer_clone.jpg — использовались только в уже удалённом src/data/cards.js, больше на них никто не ссылался

## Что можно сделать

Вообще хорошая практика создать кастомный хук useDebounce и использовать

Так же,я бы наверное компонент фильтр вынес бы отдельно и компонент с пагинацией.Внутрь передал бы нужные пропсы, чтобы им управлять.А так не критично,т.к нигде не переиспользуется

https://rickandmortyapi.com/api/character? - ссылка на API обычно выносится в переменное окружение .env , чтоб не харкодить и не дублировать

А так в целом сделано хорошо
